name: "AI Resume Screening — CV Research Intern"
description: "Screen candidate resumes against job description, select candidates for first-round interview (fictional example)"
category: hr
difficulty: hard

tags:
  - resume-screening
  - recruitment
  - f1-evaluation

evaluation:
  type: code_exec
  rubric_script: .eval/rubric.py
  pass_pattern: "result=PASSED"

agent:
  timeout_seconds: 600
  max_turns: 100
